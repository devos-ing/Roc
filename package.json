{
  "name": "agile-agents",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "agile": "./src/cli/main.ts"
  },
  "scripts": {
    "test": "mkdir -p .tmp-agile-tests && TMPDIR=$PWD/.tmp-agile-tests bun test --timeout 30000",
    "typecheck": "tsc --noEmit",
    "check": "bun run typecheck && bun run test"
  },
  "dependencies": {
    "simple-git": "^3.36.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
